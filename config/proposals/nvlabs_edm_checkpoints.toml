# Four distinct checkpoint links declared in NVIDIA's EDM README model examples.
# This deliberately does not treat the linked directories as an exhaustive index.
[[source]]
name = "nvlabs-edm-checkpoints"
adapter = "nvlabs_edm_checkpoints"
enabled = false
entry_tags = ["field:generative-image-modeling", "ecosystem:nvidia", "model-family:edm", "kind:checkpoint", "evidence:first-party-model-example-url"]
repository = "NVlabs/edm"
branch = "main"
document_path = "README.md"
max_response_bytes = 4194304
max_checkpoints = 100
