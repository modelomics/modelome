# timm v0.6.13's Vision Transformer module declares 32 direct pretrained
# checkpoint URLs for AugReg, SAM, DINO, and MIIL variants. Current timm's
# parsed registry and current Torchvision source do not declare these exact URLs.
[[source]]
name = "timm-legacy-vit-v0613"
adapter = "timm_legacy_vit"
enabled = false
entry_tags = ["field:computer-vision", "ecosystem:timm", "kind:checkpoint"]
schedule = "weekly"

[evidence]
timm_vit_source = "https://github.com/huggingface/pytorch-image-models/blob/v0.6.13/timm/models/vision_transformer.py"
timm_vit_module = "https://raw.githubusercontent.com/huggingface/pytorch-image-models/v0.6.13/timm/models/vision_transformer.py"
