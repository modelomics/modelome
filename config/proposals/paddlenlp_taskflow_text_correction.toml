# Official PaddleNLP CSCTask source declares ernie-csc's exact URL and MD5.
[[source]]
name = "paddlenlp-taskflow-text-correction-checkpoints"
adapter = "paddlenlp_taskflow_text_correction"
enabled = false
repository = "PaddlePaddle/PaddleNLP"
branch = "develop"
source_path = "paddlenlp/taskflow/text_correction.py"
max_response_bytes = 4194304

# Covers the public model_state artifact only, not the separately declared pinyin vocab.
