# BPEmb's official project README indexes 275 language pages, and those pages
# publish exact binary and text vector archive links per vocabulary/dimension.
# Disabled pending catalog-scale source review; archives are reference-only.
[[source]]
name = "bpemb-pretrained-vectors"
adapter = "bpemb_pretrained_vector_registry"
enabled = false
entry_tags = ["field:natural-language-processing", "ecosystem:bpemb", "kind:pretrained-vectors"]
repository = "bheinzerling/bpemb"
branch = "master"
provider_namespace = "bpemb:pretrained-vector"
schedule = "monthly"
max_response_bytes = 4194304
max_entries = 10000
max_languages = 300
languages_per_page = 10
