[[source]]
name = "cogact-checkpoint-registry"
adapter = "cogact_checkpoint_registry"
enabled = false
entry_tags = ["field:robotics", "ecosystem:cogact", "kind:checkpoint"]
schedule = "weekly"
max_response_bytes = 4194304
max_entries = 8

# Three first-party size variants with exact SHA-pinned Hugging Face files.
# Metadata only; checkpoint bytes are not downloaded.
