# RadiologyNET's first-party README lists ten per-model PyTorch weight archives
# with exact Google Drive IDs. Each archive is tar.gz; internal paths are unknown.
[[source]]
name = "radiologynet-first-party-checkpoints"
adapter = "radiologynet_registry"
enabled = false
entry_tags = ["field:medical-imaging", "ecosystem:radiologynet", "kind:pretrained-weights"]
repository = "AIlab-RITEH/RadiologyNET-TL-models"
branch = "master"
source_path = "README.md"
max_response_bytes = 2097152
max_entries = 100

[evidence]
model_archive_table = "https://github.com/AIlab-RITEH/RadiologyNET-TL-models/blob/master/README.md"
scope = "Ten explicitly named PyTorch model archives and their exact Google Drive file IDs; no archive member inference"
