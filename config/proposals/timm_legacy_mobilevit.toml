# timm v0.6.13's MobileViT module declares 16 direct pretrained checkpoint
# URLs for MobileViT and MobileViTv2 model variants. This proposal reads only
# that immutable release and module.
[[source]]
name = "timm-legacy-mobilevit-v0613"
adapter = "timm_legacy_mobilevit"
enabled = false
entry_tags = ["field:computer-vision", "ecosystem:timm", "kind:checkpoint"]
schedule = "weekly"

[evidence]
timm_mobilevit_source = "https://github.com/huggingface/pytorch-image-models/blob/v0.6.13/timm/models/mobilevit.py"
timm_mobilevit_module = "https://raw.githubusercontent.com/huggingface/pytorch-image-models/v0.6.13/timm/models/mobilevit.py"
